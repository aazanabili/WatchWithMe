port: 7880
bind_addresses: [0.0.0.0]
rtc:
  node_ip: 127.0.0.1
  tcp_port: 7881
  udp_port: 7882
  enable_loopback_candidate: true
  use_external_ip: false
  stun_servers: []
turn:
  # TURN is provided by the dedicated coturn service. Keeping LiveKit's
  # embedded TURN disabled avoids duplicate ICE servers and relay ranges.
  enabled: false
redis:
  address: redis:6379
